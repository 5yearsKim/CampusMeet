import React from 'react';
import CreateBoard from 'src/components/community/CreateBoard';

function CreateBoardScreen(props) {
  return (
    <CreateBoard {...props}/>
  );
}

export default CreateBoardScreen;

