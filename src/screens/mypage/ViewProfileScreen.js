import React from 'react';
import ViewProfile from 'src/components/mypage/ViewProfile';

function ViewProfileScreen(props) {
  return (
    <ViewProfile {...props}/>
  );
}

export default ViewProfileScreen;
